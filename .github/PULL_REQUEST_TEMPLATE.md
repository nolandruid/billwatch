<!-- PR title must be a Conventional Commit, e.g. "feat(subscribe): add double opt-in". -->

## What & why

<!-- What does this change do, and what problem/issue does it address? -->

Closes #

## How I tested it

<!-- Commands run, manual steps, screenshots if UI. -->

## Checklist

- [ ] `npm run lint` passes
- [ ] `npm run typecheck` passes
- [ ] `npm test` passes
- [ ] `npm run build` passes
- [ ] Code is formatted (`npm run format`)
- [ ] No secrets committed; docs updated if behaviour changed
